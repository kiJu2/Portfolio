window.onload = () =>{
	var typed = new Typed('.typed', {
		strings: ['원하는 글씨를'],
		typeSpeed: 100,
		// backSpeed: 100,
		fadeOut: false,
		// smartBackspace: true,
		cursorChar: '|',
		// loop: true
	});
}